import './section-about.styles.scss'

import SectionAboutTitle from '../section-about-title/section-about-title.component'

const SectionAbout = () => {
    return (
    <section className="section-about">
      <SectionAboutTitle />
    </section>
    )
}

export default SectionAbout